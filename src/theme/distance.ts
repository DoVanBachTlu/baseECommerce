import {fontScale} from 'react-native-utils-scale';

export const spaceHorizontal = {
  xxs: fontScale(2),
  xs: fontScale(4),
  s: fontScale(8),
  m: fontScale(10),
  l: fontScale(20),
  xl: fontScale(32),
  xxl: fontScale(64),
};

export const paddingBaseScreen = spaceHorizontal.m;
